# Terraform

## Quick start

1. Set AWS credentials in your environment.
2. Set the SSH key name:

```bash
export TF_VAR_ssh_key_name="your-key-name"
```

3. Init and apply:

```bash
terraform init
terraform apply
```
